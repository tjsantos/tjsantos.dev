name: Auto Approve
on: [deployment, deployment_status, deployment_review]

# https://docs.github.com/en/rest/actions/workflow-runs?apiVersion=2022-11-28#review-pending-deployments-for-a-workflow-run
#
jobs:
  auto-approve:
    # if: github.event.deployment.environment == 'cloudflare' && github.event.deployment.ref == 'refs/heads/main'
    if: github.event_name == 'deployment_review' || github.event.deployment.environment == 'approve-test-e2e'
    runs-on: ubuntu-latest
    # echo github context for debugging: environment, ref
    steps:
      - name: Echo GitHub context
        env:
          GITHUB_EVENT: ${{ toJson(github.event) }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_REF: ${{ github.ref }}
        run: |
          echo "GITHUB_EVENT_NAME: $GITHUB_EVENT_NAME"
          echo "GITHUB_REF: $GITHUB_REF"
          echo "GITHUB_EVENT: $GITHUB_EVENT"

      - uses: actions/checkout@v5
      - uses: actions/setup-node@v5
        with:
          node-version: lts/*
          cache: npm
      - name: Install dependencies
        run: npm ci --workspace=.github/scripts

      - name: Run auto approve script
        run: npm exec --workspace=.github/scripts -- node approve.ts
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_EVENT: ${{ github.event }}

      - name: Error message on failure
        if: failure()
        run: echo "::error::Auto-approval failed. Please manually approve the deployment."
